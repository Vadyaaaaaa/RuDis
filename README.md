# RuDis - Discord Clone

Клон Discord с современным интерфейсом и функциональностью реального времени.

## Возможности

- 🔐 Аутентификация пользователей (регистрация и вход)
- 🏠 Создание и управление серверами
- channel Создание текстовых каналов
- 💬 Отправка сообщений в реальном времени
- ⚡ WebSocket для мгновенной доставки сообщений
- 🎨 Современный UI в стиле Discord

## Технологии

### Backend
- Node.js + Express
- Socket.io для WebSocket соединений
- JWT для аутентификации
- bcrypt для хеширования паролей
- JSON файл для хранения данных

### Frontend
- React 18
- React Router для навигации
- Socket.io-client для реального времени
- Axios для HTTP запросов
- CSS для стилизации

## Установка

1. Установите зависимости для всего проекта:
```bash
npm run install-all
```

2. Запустите сервер и клиент одновременно:
```bash
npm run dev
```

Или запустите отдельно:

Backend (порт 5000):
```bash
npm run server
```

Frontend (порт 3000):
```bash
npm run client
```

## Использование

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Зарегистрируйте новый аккаунт или войдите
3. Создайте сервер или выберите существующий
4. Создайте канал в сервере
5. Начните общаться!

## Структура проекта

```
rudis/
├── server/
│   ├── index.js          # Главный сервер
│   ├── routes/           # API маршруты
│   ├── middleware/       # Middleware (auth)
│   └── database/         # База данных (JSON)
├── client/
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── context/      # React Context
│   │   └── App.js        # Главный компонент
│   └── public/
└── package.json
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Серверы
- `GET /api/servers` - Получить серверы пользователя
- `GET /api/servers/:id` - Получить сервер по ID
- `POST /api/servers` - Создать сервер

### Каналы
- `GET /api/channels/server/:serverId` - Получить каналы сервера
- `GET /api/channels/:id` - Получить канал по ID
- `POST /api/channels` - Создать канал

### Сообщения
- `GET /api/messages/channel/:channelId` - Получить сообщения канала
- `POST /api/messages` - Отправить сообщение

## Socket.io Events

- `join-server` - Присоединиться к серверу
- `join-channel` - Присоединиться к каналу
- `send-message` - Отправить сообщение
- `new-message` - Новое сообщение (broadcast)
- `typing` - Пользователь печатает
- `stop-typing` - Пользователь перестал печатать

## Примечания

- Данные хранятся в JSON файле (`server/database/data.json`)
- Для продакшена рекомендуется использовать настоящую БД (MongoDB, PostgreSQL)
- JWT секрет должен быть изменен в продакшене
- Порт сервера: 5000, клиента: 3000

## Лицензия

MIT

