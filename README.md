# RuDis - Discord Clone

Клон Discord с функциональностью друзей, серверов и каналов.

## 🚀 Возможности

- ✅ Регистрация и вход по нику (без пароля)
- Создание серверов и каналов
- Обмен сообщениями в реальном времени (Socket.io)
- Система друзей:
  - Поиск пользователей
  - Отправка запросов в друзья
  - Принятие/отклонение запросов
  - Удаление из друзей
- Современный UI в стиле Discord

## 📋 Требования

- Node.js (v14 или выше)
- npm или yarn

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Vadyaaaaaa/RuDis.git
cd RuDis
```

2. Установите зависимости для сервера:
```bash
npm install
cd server
npm install
cd ..
```

3. Установите зависимости для клиента:
```bash
cd client
npm install
cd ..
```

## 🚀 Запуск

Запустите сервер и клиент одновременно:
```bash
npm run dev
```

- Backend будет доступен на: http://localhost:5000
- Frontend будет доступен на: http://localhost:3000

## 📁 Структура проекта

```
RuDis/
├── server/          # Backend (Node.js + Express)
│   ├── routes/      # API маршруты
│   ├── database/    # JSON база данных
│   ├── middleware/  # Middleware (аутентификация)
│   └── services/    # Сервисы (email)
├── client/          # Frontend (React)
│   └── src/
│       ├── components/  # React компоненты
│       └── context/     # React Context
└── package.json
```

## 🔧 Технологии

- **Backend:**
  - Node.js
  - Express
  - Socket.io
  - JWT для аутентификации
  - JSON файл для хранения данных

- **Frontend:**
  - React
  - React Router
  - Socket.io-client
  - Axios

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Друзья
- `GET /api/friends` - Список друзей
- `GET /api/friends/requests` - Входящие запросы
- `POST /api/friends/request` - Отправить запрос
- `POST /api/friends/accept/:requestId` - Принять запрос
- `POST /api/friends/reject/:requestId` - Отклонить запрос
- `DELETE /api/friends/:friendId` - Удалить из друзей
- `GET /api/friends/search?q=...` - Поиск пользователей

### Серверы и каналы
- `GET /api/servers` - Список серверов
- `POST /api/servers` - Создать сервер
- `GET /api/channels/server/:serverId` - Каналы сервера
- `POST /api/channels` - Создать канал
- `GET /api/messages/channel/:channelId` - Сообщения канала
- `POST /api/messages` - Отправить сообщение

## 🌐 Развертывание

### Vercel (Frontend)
1. Установите Vercel CLI: `npm i -g vercel`
2. В папке `client`: `vercel`

### Heroku (Backend)
1. Установите Heroku CLI
2. `heroku create your-app-name`
3. `git push heroku main`

### Railway / Render
Также можно использовать Railway или Render для развертывания.

## 📄 Лицензия

MIT

## 👤 Автор

Vadyaaaaaa

---

**Примечание:** Это учебный проект. Для продакшена рекомендуется использовать настоящую базу данных (MongoDB, PostgreSQL) вместо JSON файла.
